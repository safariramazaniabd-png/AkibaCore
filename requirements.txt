# AkibaCore — Dépendances Python
# Aucun paquet externe requis.
# Tkinter et SQLite sont inclus dans la distribution standard de Python 3.
#
# Pour installer tkinter sous Linux si absent :
#   sudo apt install python3-tk   (Ubuntu/Debian/Linux Mint)
#   sudo dnf install python3-tkinter  (Fedora)
#   sudo pacman -S tk              (Arch Linux)
#
# Python >= 3.8 requis.
